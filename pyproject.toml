[project]
name = "min-claude-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "him-agni", email = "himaniagrawal507@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
min-claude-code = "min_claude_code:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
